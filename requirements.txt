# 后端运行依赖（对应 doc/design-detail.md 6.3）
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
redis>=5.0
celery>=5.3
httpx>=0.27
PyJWT>=2.8
cryptography>=42.0
Pillow>=10.0
python-multipart>=0.0.9

# 生产数据库（本地开发默认 SQLite）
psycopg2-binary>=2.9

# S3 兼容对象存储（阿里云 OSS/COS，storage_provider=s3 时安装）
# boto3>=1.34

# 测试
pytest>=8.0